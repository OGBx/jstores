export default async function AdminLogin({ searchParams }:{ searchParams:Promise<{error?:string;next?:string}> }) {
  const params = await searchParams;
  return <main className="container-shell grid min-h-screen place-items-center py-12"><form action="/api/admin/login" method="post" className="card w-full max-w-md space-y-5 p-6 sm:p-8"><div><p className="text-sm font-bold text-emerald-600">J-STORES ADMIN</p><h1 className="mt-1 text-3xl font-black">Sign in</h1><p className="mt-2 text-sm opacity-65">Use the administrator account configured on the server.</p></div>{params.error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-700">Invalid email or password.</p>}<input type="hidden" name="next" value={params.next || '/admin'}/><label className="block"><span className="mb-1.5 block text-sm font-semibold">Email</span><input className="input" type="email" name="email" required autoComplete="username"/></label><label className="block"><span className="mb-1.5 block text-sm font-semibold">Password</span><input className="input" type="password" name="password" required autoComplete="current-password"/></label><button className="btn-primary w-full">Sign in</button></form></main>;
}
